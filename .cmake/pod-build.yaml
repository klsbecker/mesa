---
apiVersion: v1
kind: Pod
metadata:
  name: ungsw-mesa-build-env
spec:
  containers:
  - name: pod-mesa-build
    image: artifacts-aws.devops.microchip.com:7996/ung/mesa/mesa-build-env@<none>
    imagePullPolicy: IfNotPresent
    command:
    - cat
    tty: true
    resources:
      requests:
        cpu: '30'
        memory: 60Gi
      limits:
        cpu: '30'
        memory: 60Gi
